"use strict";
let geneticist = Math.random() > 0.5
    ? "brandom"
    : undefined;
if (geneticist) {
    geneticist.toUpperCase();
    console.log(typeof geneticist);
}
else {
    console.log(typeof geneticist);
}
// geneticist.toUpperCase();
